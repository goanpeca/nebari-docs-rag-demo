# Required API Keys
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Demo Login Credentials (for protecting the demo)
# WARNING: Change these before deploying! These are defaults for local development only.
DEMO_USERNAME=your-username
DEMO_PASSWORD=your-secure-password

# Paths
NEBARI_DOCS_PATH=/path/to/nebari-docs

# Optional: Base directory for file paths (defaults to app directory if not set)
# Set this when deploying in environments where working directory differs from app location.
# BASE_DIR=/path/to/nebari-docs-rag-demo

# Optional: Advanced embedding providers (NOT NEEDED - app uses free local embeddings)
# VOYAGE_API_KEY=pa-...  # Only if you want to use Voyage AI embeddings
# OPENAI_API_KEY=sk-...  # Only if you want to use OpenAI embeddings
